"""Worked examples of solution architectures on the PROCESS model graph, one folder per
case. Each notebook explains the architecture, builds it as a short list of graph
operations, shows the resulting run order as a DSM, and runs it on the Helias
stellarator. `notebook_tools` runs the notebooks headlessly; `tests/examples` checks
them.
"""
