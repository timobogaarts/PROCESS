# Generated by `$PY functional_process/run_cold_matrix.py --native --compare-process --slsqp --out functional_process/reference_slsqp_matrix.txt`; do not hand-edit
# the table -- a re-run overwrites it, this header included.
#
# MEASURED 2026-09-05 17:17.
# SEEDED    `--native` -- where every starting value came from.
# DRIVEN BY scipy SLSQP (`--slsqp`) -- which optimiser answered every `Optimise`.
# SCORED    against PROCESS's converged answer (`PRO`/`PRO objf`/`d objf`/`worst dx`).
#   These are TWO AXES. A `--native` row that is scored still had no PROCESS object
#   in its solve path; the answer is loaded afterwards, for the columns only.
# TREE: HEAD c7453984 (Run SLSQP over the whole matrix, and correct two claims it broke)
#   Every file in `PORT_FILES` is clean, so these rows are that commit's.
#
# A row is one configuration under BOTH formulations; a failure is a row and
# never an exit, so a `REFUSED`/`FAILED`/`no-step` cell is a measurement with
# its reason in the NOTES block, not a gap in the run.

COLD MATRIX -- MDF and SAND from each input file's own cold start

         configuration   seed  form  graph  nodes  desg  cond   eq  blks  drvn   SQP      status            objf        PRO objf    d objf  worst dx    max|eq|      min ie  PRO
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
    stellarator_helias    nat   MDF    154    169     8    15    2   158     5    27   converged      1.21848284      1.21491678  2.94e-03  1.09e-01   4.85e-10   -1.47e-09   46
    stellarator_helias    nat  SAND    154    124    14    21    8    46    14   501    cap(500)      1.21846128      1.21491678  2.92e-03  1.19e-01   3.95e-04   -3.54e-08   46
             helias_5b    nat   MDF    154    160     3     6    3   149     5     2     stopped     0.758330471     0.763518372  6.79e-03  1.91e-02   1.16e-01    1.10e-01    3
             helias_5b    nat  SAND    154     96     9    12    9    65     9     2     stopped      1.04968527     0.763518372  3.75e-01  1.91e-02   3.92e+03    1.10e-01    3
     large_tokamak_nof    nat   MDF    247    275    20    27    3   249     6     8   converged             1.6             1.6  1.74e-11  2.00e-02   7.04e-12   -1.32e-12    8
     large_tokamak_nof    nat  SAND    247    180    27    34   10    96    27    13   converged             1.6             1.6  1.73e-11  7.16e-01   1.39e-13   -1.75e-14    8
    large_tokamak_eval    nat   MDF    249    275     2     2    2    25     2     3   converged               -            none         -  3.84e-08   3.54e-14   -1.99e+00    0
 low_aspect_ratio_DEMO    nat   MDF    247    275    19    26    4   251     6    12   converged    -0.406312732     -0.40629623  4.06e-05  1.15e-04   4.44e-15   -9.76e-13   16
 low_aspect_ratio_DEMO    nat  SAND    247    184    26    33   11    92    26    17   converged    -0.399155367     -0.40629623  1.76e-02  7.09e-02   1.70e-11   -3.03e-10   16
spherical_tokamak_eval    nat   MDF    244    261     3     3    3    29     2     2   converged               -            none         -  2.63e-08   1.16e-09   -1.38e+01    0
         st_regression    nat   MDF    244    264    14    19    3   251     4    11   converged     -16.5885767     -16.5885765  1.06e-08  4.49e-04   7.64e-12   -1.86e-10   10
         st_regression    nat  SAND    244    159    20    25    9   107    20    10   converged     -16.5885767     -16.5885765  1.04e-08  1.59e-02   8.00e-14   -4.24e-13   10

`nodes/blks/drvn` differ by formulation and are not comparable across the two lines:
  MDF  nodes = graph + conditions, blks/drvn = the inner MDA's blocking
  SAND nodes = the one Drive's nodes, blks = schedule steps, drvn = the block's unknowns
`desg/cond/eq` are the optimiser's own shape and ARE comparable: MDF's design is PROCESS's `ixc` exactly; SAND's adds the coupling unknowns and the residual equalities that hold them.
`max|eq|` is in `VmconDriver.condition_scale`'s units (SAND's residuals relative, PROCESS's own constraints at factor 1.0). `min ie` is VMCON's sign: NEGATIVE IS VIOLATED.
`PRO` is PROCESS's own solver iteration count for a converged solve of the same file.

TWO INDEPENDENT AXES, and this table keeps them apart on purpose. `seed` is where the
STARTING STATE came from; the `PRO*` columns are whether the finished answer was SCORED
against PROCESS's. `seed nat` with a filled `PRO objf` means no PROCESS object was in the
solve path at all and PROCESS's converged answer was loaded afterwards, for scoring only
(`run_one`'s `oracle`, `--compare-process`). A blank `PRO*` group means no comparison was
asked for -- it is NOT evidence that the row was seeded natively; read the `seed` column.

`PRO objf`/`d objf`/`worst dx` compare the port with PROCESS'S OWN ANSWER, which is the
only comparison on this table that is not the port against itself under another seeding mode:
  PRO objf  `objective_function(i_figure_merit, converged data)`. `none` means PROCESS FORMED NO
            OBJECTIVE -- the file states `i_process_run_mode = -2`, so `_Fsolve.solve` ends
            `self.objf = None`. `-` means NO COMPARISON WAS ASKED FOR (`--no-compare-process`,
            or `--native` without `--compare-process`) -- a statement about this column, not
            about the seed.
  d objf    |port - PROCESS| / |PROCESS| on that objective. A row flagged EXPLAINED in the
            notes is a DOCUMENTED disagreement, not a regression (`EXPLAINED_OBJECTIVE_READS`).
  worst dx  the largest |port - PROCESS| / |PROCESS| over the `ixc` design vector -- the same
            quantity `run_sand_harness.py`'s per-variable table prints as `rel`. The notes
            block says which iteration variable it occurred at.

BOUNDARY VALUES -- mode native; `supplied` is every boundary path but the
`solver` and `guess` rows, which no provider could answer (`paths` is the raw total).

         configuration  provider   seed   held  none  supplied  paths
    stellarator_helias       566      0      0     0       566    566
             helias_5b       566      0      0     0       566    566
     large_tokamak_nof       566      0      0     0       566    566
    large_tokamak_eval       566      0      0     0       566    566
 low_aspect_ratio_DEMO       566      0      0     0       566    566
spherical_tokamak_eval       566      0      0     0       566    566
         st_regression       566      0      0     0       566    566

PHASE TIMINGS -- seconds per arm, EXCLUSIVE. `model`/`other` UNVERIFIED -- see `_timing_block`.
`model` is graph evaluation inside the driver, `sqp` the optimiser's own cost (cvxpy, CLARABEL,
the line search). `other` is the residual: graph assembly, the cached PROCESS load, dispatch.

         configuration  form     trace     lower   compile     model       sqp     other     total
    stellarator_helias  MDF       1.6       1.1      10.2       2.3       0.0       1.1      16.2
    stellarator_helias SAND       0.7       1.2      12.0       3.2       0.0       5.1      22.2
             helias_5b  MDF       1.8       1.0       9.9       1.8       0.0       0.9      15.3
             helias_5b SAND       0.4       1.1       8.7       1.2       0.0       1.7      13.1
     large_tokamak_nof  MDF       4.3       2.8      36.8       7.3       0.0       2.7      53.9
     large_tokamak_nof SAND       1.8       3.6      43.6       3.8       0.0       6.1      58.8
    large_tokamak_eval  MDF       2.9       2.0      19.4       0.0       0.0       4.7      28.9
 low_aspect_ratio_DEMO  MDF       4.4       2.9      39.3       7.4       0.0       2.7      56.7
 low_aspect_ratio_DEMO SAND       1.4       3.6      44.8       5.7       0.0       5.9      61.4
spherical_tokamak_eval  MDF       2.2       1.6      16.9       0.0       0.0       4.3      25.0
         st_regression  MDF       2.7       2.1      24.0       5.1       0.0       1.9      35.8
         st_regression SAND       0.6       2.6      30.2       3.3       0.0       4.3      41.0

NOTES
  stellarator_helias MDF: worst dx 1.09e-01 at ixc 109 (over 8 design variable(s), against PROCESS's own converged x)
  stellarator_helias MDF: objective gap 2.94e-03 is NOT labelled explained, and the reason is measured: the design vector moved 1.09e-01 (ixc 109), so the two objectives were evaluated at two DIFFERENT points and the difference is not evidence about either. This objective does read a path downstream of `mda_harness.EXPLAINED_DISAGREEMENTS['.heat_transport.p_plant_electric_base_total_mw']`, so part of the gap may be that chain -- but separating the two needs the port's objective at PROCESS'S OWN x, which is a Stage A measurement (`run_sand_harness.py`) and not a matrix row.
  stellarator_helias SAND: worst dx 1.19e-01 at ixc 56 (over 8 design variable(s), against PROCESS's own converged x)
  stellarator_helias SAND: objective gap 2.92e-03 is NOT labelled explained, and the reason is measured: the design vector moved 1.19e-01 (ixc 56), so the two objectives were evaluated at two DIFFERENT points and the difference is not evidence about either. This objective does read a path downstream of `mda_harness.EXPLAINED_DISAGREEMENTS['.heat_transport.p_plant_electric_base_total_mw']`, so part of the gap may be that chain -- but separating the two needs the port's objective at PROCESS'S OWN x, which is a Stage A measurement (`run_sand_harness.py`) and not a matrix row.
  helias_5b MDF: worst dx 1.91e-02 at ixc 4 (over 3 design variable(s), against PROCESS's own converged x)
  helias_5b MDF: objective gap 6.79e-03 is NOT labelled explained, and the reason is measured: the design vector moved 1.91e-02 (ixc 4), so the two objectives were evaluated at two DIFFERENT points and the difference is not evidence about either. This objective does read a path downstream of `mda_harness.EXPLAINED_DISAGREEMENTS['.heat_transport.p_plant_electric_base_total_mw']`, so part of the gap may be that chain -- but separating the two needs the port's objective at PROCESS'S OWN x, which is a Stage A measurement (`run_sand_harness.py`) and not a matrix row.
  helias_5b SAND: worst dx 1.91e-02 at ixc 4 (over 3 design variable(s), against PROCESS's own converged x)
  helias_5b SAND: objective gap 3.75e-01 is NOT labelled explained, and the reason is measured: the design vector moved 1.91e-02 (ixc 4), so the two objectives were evaluated at two DIFFERENT points and the difference is not evidence about either. This objective does read a path downstream of `mda_harness.EXPLAINED_DISAGREEMENTS['.heat_transport.p_plant_electric_base_total_mw']`, so part of the gap may be that chain -- but separating the two needs the port's objective at PROCESS'S OWN x, which is a Stage A measurement (`run_sand_harness.py`) and not a matrix row.
  large_tokamak_nof MDF: worst dx 2.00e-02 at ixc 57 (over 20 design variable(s), against PROCESS's own converged x)
  large_tokamak_nof SAND: worst dx 7.16e-01 at ixc 135 (over 20 design variable(s), against PROCESS's own converged x)
  large_tokamak_eval MDF: RootFind over 2 equality/-ies, 23 inequality/-ies evaluated at the answer and not driven -- PROCESS's own `fsolve` shape. Stated IN the graph: the blocking drives 35 of 275 nodes
  large_tokamak_eval MDF: worst dx 3.84e-08 at ixc 6 (over 2 design variable(s), against PROCESS's own converged x)
  large_tokamak_eval: this file states a ROOT FIND (`i_process_run_mode = -2`), so it gets ONE row and not two. PROCESS root-finds the equalities alone with `scipy.optimize.fsolve`, forms no objective and never examines the inequalities; MDF's design vector IS PROCESS's `ixc`, so this row poses exactly PROCESS's own square system (`mdf.assemble(root_find=True)`). MDF-against-SAND is a split between two ways of distributing an OPTIMISATION, and this file states none -- a SAND row here would solve a larger square system over design and coupling that PROCESS never writes down. The inequalities are still evaluated once at the answer, exactly as PROCESS evaluates them, and reported through `Mdf.reported`.
  low_aspect_ratio_DEMO MDF: worst dx 1.15e-04 at ixc 57 (over 19 design variable(s), against PROCESS's own converged x)
  low_aspect_ratio_DEMO SAND: worst dx 7.09e-02 at ixc 41 (over 19 design variable(s), against PROCESS's own converged x)
  spherical_tokamak_eval MDF: RootFind over 3 equality/-ies, 15 inequality/-ies evaluated at the answer and not driven -- PROCESS's own `fsolve` shape. Stated IN the graph: the blocking drives 39 of 261 nodes
  spherical_tokamak_eval MDF: worst dx 2.63e-08 at ixc 6 (over 3 design variable(s), against PROCESS's own converged x)
  spherical_tokamak_eval: this file states a ROOT FIND (`i_process_run_mode = -2`), so it gets ONE row and not two. PROCESS root-finds the equalities alone with `scipy.optimize.fsolve`, forms no objective and never examines the inequalities; MDF's design vector IS PROCESS's `ixc`, so this row poses exactly PROCESS's own square system (`mdf.assemble(root_find=True)`). MDF-against-SAND is a split between two ways of distributing an OPTIMISATION, and this file states none -- a SAND row here would solve a larger square system over design and coupling that PROCESS never writes down. The inequalities are still evaluated once at the answer, exactly as PROCESS evaluates them, and reported through `Mdf.reported`.
  st_regression MDF: worst dx 4.49e-04 at ixc 93 (over 14 design variable(s), against PROCESS's own converged x)
  st_regression SAND: worst dx 1.59e-02 at ixc 93 (over 14 design variable(s), against PROCESS's own converged x)
